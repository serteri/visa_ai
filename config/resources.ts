/**
 * config/resources.ts
 *
 * Single source of truth for external resource URLs.
 * Import from here rather than hard-coding URLs in components.
 */

/** Official 2026 Skilled Occupation List (SOL) — served from the local
 *  public directory. Rename the file → update only this constant. */
export const OCCUPATION_LIST_2026_PDF_URL =
  "/australia-skilled-occupation-list-2026.pdf";
